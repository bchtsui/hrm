<%= form_for(@rank_history) do |f| %>
  <% if @rank_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rank_history.errors.count, "error") %> prohibited this rank_history from being saved:</h2>

      <ul>
      <% @rank_history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rank %><br>
    <%= f.text_field :rank %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
